[pytest]
DJANGO_SETTINGS_MODULE = config.settings.development
python_files = tests.py test_*.py *_tests.py
testpaths = accounts equipment requests notifications core
addopts = 
    --tb=short
    -q
    --disable-warnings
    --reuse-db
